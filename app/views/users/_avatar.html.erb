<% if resource.avatar.attached?%>
    <%= image_tag resource.avatar, class: "rounded-circle", width: 100, height: 100%>
<% else %>
    <%= image_tag gravatar_image_url('aseighali96@gmail.com'.gsub('spam', 'mdeering'),size: 100 ), class: "rounded-circle"%>
<% end %>